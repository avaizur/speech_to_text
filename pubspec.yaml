name: lectura
description: A new Flutter project.

publish_to: 'none' # Remove this line if you intend to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  speech_to_text: ^6.6.2
  http: ^1.2.2
  path_provider: ^2.0.15
  open_filex: ^4.5.0
  async: 2.11.0 # Align with flutter_test
  characters: 1.3.0 # Align with flutter_test
  clock: 1.1.1 # Align with flutter_test
  collection: 1.18.0 # Align with flutter_test
  http_parser: ^4.0.2 # Downgraded for compatibility
  material_color_utilities: 0.11.1 # Align with flutter_test
  meta: 1.15.0 # Align with flutter_test
  path: 1.9.0 # Align with flutter_test
  string_scanner: 1.2.0 # Align with flutter_test
  app_links: ^4.0.0 # Replaced uni_links with app_links for compatibility
  url_launcher: ^6.1.11

dev_dependencies:
  flutter_test:
    sdk: flutter
  boolean_selector: 2.1.1 # Align with flutter_test
  fake_async: 1.3.1 # Align with flutter_test
  leak_tracker: 10.0.5 # Align with flutter_test
  leak_tracker_flutter_testing: 3.0.5 # Align with flutter_test
  matcher: 0.12.16+1 # Align with flutter_test
  stack_trace: 1.11.1 # Align with flutter_test
  test_api: 0.7.2 # Align with flutter_test
  vm_service: 14.2.5 # Downgraded for compatibility

flutter:
  uses-material-design: true
